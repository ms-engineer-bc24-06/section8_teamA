version: "3"
services:
  client:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    command: sh -c "cd frontapp && npm run start"
    ports:
      - "3000:3000"